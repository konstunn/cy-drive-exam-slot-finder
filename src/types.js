// Shared types and interfaces

/**
 * @typedef {Object} Credentials
 * @property {string} username
 * @property {string} password
 */

/**
 * @typedef {Object} ExamTimeSlot
 * @property {Date} time
 * @property {string} examCenterCity
 * @property {string} licenseCategory
 */

/**
 * @typedef {Object} ExamDetails
 * @property {string} centerCity
 * @property {string} carPlateNumber
 */

module.exports = {
  // Types are defined using JSDoc for TypeScript-like documentation
};